# Custom Homebrew Taps

Mainly contains formulae for enabling cargo features.

## Installation

```shell
brew install ziyong-was-taken/tap/<formula>
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
